Написать маленькую программу, которая запрашивает имя пользователя, проверяет есть ли хэш этого имени в файле с именами,
если есть - пишет есть, если нет, предлагает сохранить, если пользователь соглашается, то хэш нового имени сохраняется
в файл, иначе программа начинает работу заново.